# TEMPLATE ONLY - do not put real values here or commit real secrets.
# Apply real secrets directly with kubectl instead, e.g.:
#   kubectl create secret generic campus-app-secrets \
#     --from-literal=DB_PASSWORD=<real-value> \
#     --from-literal=API_KEY=<real-value> \
#     -n production
apiVersion: v1
kind: Secret
metadata:
  name: campus-app-secrets
  namespace: production
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  API_KEY: "REPLACE_ME"
