# TEMPLATE ONLY - do not put real values here.
# Apply real secrets directly with kubectl, e.g.:
#   kubectl create secret generic campus-app-secrets \
#     --from-literal=DB_PASSWORD=<real-value> \
#     -n production
apiVersion: v1
kind: Secret
metadata:
  name: campus-app-secrets
  namespace: production
type: Opaque
stringData:
  DB_PASSWORD: "REPLACE_ME"
  API_KEY: "REPLACE_ME"
